type Product {
  id: ID!
  name: String!
  price: Int!
  unit: String!
  category: String
}

type Query {
  getProducts: [Product]
  getProductById(id: ID!): Product
}

type Mutation {
  createProduct(name: String!, price: Int!, unit: String!, category: String): Product
}
