# Anugerah Resto Inventory Service - Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
PORT=4002

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=inventory_user
DB_PASSWORD=inventory_pass
DB_NAME=inventory_db

# ===========================================
# TOKO SEMBAKO INTEGRATION (via ngrok)
# ===========================================
# Minta URL ngrok dari kelompok Toko Sembako 
# Contoh: https://abc123.ngrok.io

# Option 1: Single base URL (jika mereka expose semua services via satu ngrok)
TOKO_SEMBAKO_URL=http://localhost

# Option 2: Individual service URLs (jika mereka expose setiap service terpisah)
# TOKO_SEMBAKO_PRODUCT_URL=https://abc123.ngrok.io/graphql
# TOKO_SEMBAKO_INVENTORY_URL=https://def456.ngrok.io/graphql
# TOKO_SEMBAKO_ORDER_URL=https://ghi789.ngrok.io/graphql

# JWT Secret (for auth)
JWT_SECRET=your_jwt_secret_key_here
